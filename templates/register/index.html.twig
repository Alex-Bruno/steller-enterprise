{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="row mt-3">
        <div class="col text-center">
            <h1 class="h3 mb-3 font-weight-normal">Register</h1>
        </div>
    </div>

    {{ include('register/_form.html.twig') }}

    <div class="row mt-2">
        <div class="col-8 offset-2 float-right">
            <a class="float-right" href="{{ path('app_login') }}">Back to login</a>
        </div>
    </div>

{% endblock %}
